<%# Admin — New Team %>

<div class="max-w-lg">
  <div class="flex items-center gap-2 text-sm text-gray-400 mb-7">
    <%= link_to "Teams", admin_teams_path(@current_company.slug), class: "hover:text-gray-600 transition-colors" %>
    <span>/</span>
    <span class="text-gray-700 font-medium">New team</span>
  </div>
  <h1 class="text-2xl font-semibold text-gray-900 mb-8">New Team</h1>
  <%= render "form", team: @team %>
</div>
